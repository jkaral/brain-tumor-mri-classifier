# Notebooks

Export the final Colab training and evaluation notebook into this directory before publishing the repository. Remove redundant exploratory cells, runtime logs, Drive-specific paths and any embedded credentials. The notebook should still preserve enough output to substantiate the reported training and evaluation results.

